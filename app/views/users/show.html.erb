<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section>

      <h2><%= @user.name %>の投稿<h2>
        <ul>
          <% @user.contributions.each do |c| %>
            <li><%= c.title %></li>
          <% end %>
        </ul>

    </section>
  </aside>
</div>
